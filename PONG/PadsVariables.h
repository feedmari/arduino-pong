extern bool drawPad1; //variable used from PadTask to comunicate to PrintPadsTask that Pad1 position is changed and it has to draw it.
extern bool drawPad2; //variable used from PadTask to comunicate to PrintPadsTask that Pad2 position is changed and it has to draw it.
